1-1. 接続チェーンとDEX:

Arbitrum One / Uniswap V3 で確定ですか？
はい
1-2. 対象ペアと手数料ティア:

USDC / WETH (0.05%) で確定ですか？（設定で変更可能にしますか？）
設定で変更可能
1-3. 開発言語・ライブラリ:

TypeScript (Node.js) + ethers.js v5 で確定ですか？
はい
主要機能はapiとして実装し，フロントやバックからapiを呼ぶ感じ

2. ロジック・戦略（Botの頭脳）
2-1. レンジ幅（Tick Range）:

現在価格からプラスマイナス何Tick（または何％）で設定しますか？
設定で変更可能

（例: ±50 ticks = 約±0.5%）

2-2. リバランスの発動条件:

範囲外（Out of Range）になったら即座に実行しますか？ それとも一定時間（例: 5分間）外れたままなら実行しますか？（「リバランス貧乏」対策）
設定で変更可能
2-3. リバランス時の資産配分:

一度全額回収し、**「50:50（金額ベース）」**になるように不足分をスワップして、現在価格を中心に再構築しますか？
はい
2-4. スワップ時のスリッページ許容値:

自動スワップ時のスリッページ（価格乖離）は何％まで許容しますか？（例: 0.5%）
設定で変更可能

3. 資金管理と損益計算（Botの家計簿）
3-1. 利益計算の基準:

ログに出力する「勝ち負け」の判定基準はどれにしますか？

A: 対ガチホ比較 (Vs HODL) ... 推奨（相場変動を除外したBotの実力）

B: 時価総額 (Net Value) ... ドル換算で増えたか減ったか

C: USDC本位 ... 解除時に全額USDCにする（スワップコスト高）

B

3-2. 手数料の扱い:

回収した手数料は**「再投資（複利運用）」しますか？ それとも「別ウォレットに退避（単利運用）」**または単にウォレットに残しますか？
単に残す

4. システムアーキテクチャ（Botの体）
4-1. ノード接続方式:

これまでの議論通り、**「データ取得はHTTP、イベント監視はWSS」**のハイブリッド構成にしますか？
はい

4-2. 稼働環境:

ローカルPC（Mac）ですか？ それともVPS（Linux）ですか？

プロセス管理ツール（PM2など）の使用を前提にしますか？
開発はローカル
本番はRailway＋vercel

4-3. 通知機能:

ログはコンソール出力だけで良いですか？

DiscordやTelegramへの通知機能が必要ですか？

フロントにも表示する．利確（リバランス時）はDiscordも

5. 安全装置（キルスイッチ）
5-1. ガス代リミット:

Arbitrumは安いですが、もしガス代が高騰した場合（例: > 0.5 ETH）、リバランスを中止して待機する機能は必要ですか？
不要だけどガス代は監視する

5-2. 緊急停止ライン:

総資産が開始時から 何％減ったら Botを完全停止する（損切り）などの機能は必要ですか？
はい